import { Table } from '@mui/material'
import React from 'react'

function useTable(records, hjeadCells) {

    const tablecontainer = props => (
        <Table>
        </Table>
    )

  return (
    <div>useTable</div>
  )
}

export default useTable